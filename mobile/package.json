{
  "name": "gymos-android",
  "version": "1.0.0",
  "private": true,
  "description": "GymOS Android app: a Capacitor shell around the production GymOS web app.",
  "scripts": {
    "build:www": "node scripts/build-www.mjs",
    "sync": "npm run build:www && cap sync android",
    "apk:debug": "npm run sync && node scripts/gradle.mjs assembleDebug",
    "apk:release": "npm run sync && node scripts/gradle.mjs assembleRelease",
    "test": "node --test test/*.test.js",
    "test:android": "node scripts/gradle.mjs testDebugUnitTest"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2",
    "@capacitor/share": "8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
